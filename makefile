README.md: guessinggame.sh
	echo "# Guessing Game" > README.md
	echo "" >> README.md
	echo "This is a simple Bash script that prompts the user to guess the number of files in the current directory. The game continues until the user guesses correctly, providing feedback on whether their guess is too high or too low." >> README.md
	echo "" >> README.md
	echo "## Date and Time" >> README.md
	echo "The make command was run on: `date`" >> README.md
	echo "" >> README.md
	echo "## Lines of Code" >> README.md
	echo "The guessinggame.sh script contains `wc -l < guessinggame.sh` lines of code." >> README.md
